[ssh_connection]
ssh_args = -o "ForwardAgent yes" -o "StrictHostKeyChecking no" -o "ControlMaster auto" -o "ControlPersist 10h"

[defaults]
roles_path = roles/
localhost ansible_python_interpreter="/usr/bin/env python"
retries = 6
timeout = 60
